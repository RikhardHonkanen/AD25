#ifndef PRIME_H
#define PRIME_H

#include <stdint.h>
#include <stdbool.h>

/**
 * @brief This function is used to check if number is a prime number or not.
 *
 * @param number The number to check
 * @return bool true if number if prime; otherwise false
 */
bool prime_check(uint32_t number);

#endif /* PRIME_H */
